dependencies:
  pre:
    - mkdir -p .support && cd .support && wget http://www.cmake.org/files/v3.3/cmake-3.3.1-Linux-x86_64.tar.gz && tar xzf cmake-3.3.1-Linux-x86_64.tar.gz && rm -f cmake-3.3.1-Linux-x86_64.tar.gz
  override:
    - make cmake=.support/bin/cmake install_tidy
test:
  override:
    - make check
